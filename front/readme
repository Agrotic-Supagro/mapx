Pour passer de la version locale à la version serveur du front 
-> aller dans le code source
-> dossier src
-> dossier services
-> au début des fichiers db_interactions.js + backend_services.js 
-> modifier la variable baseURL 
    -> version locale : mettre l'adresse avec localhost
    -> version serveur : mettre l'adresse serveur
